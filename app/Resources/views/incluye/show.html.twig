{% extends 'base.html.twig' %}

{% block body %}
    <h1>Incluye</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ incluye.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ incluye.nombre }}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ incluye.valor }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('incluye_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('incluye_edit', { 'id': incluye.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
